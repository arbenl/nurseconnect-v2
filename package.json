{
  "name": "nurseconnect-v2",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "type-check": "turbo run type-check",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev --parallel",
    "emulators:start": "firebase emulators:start",
    "agents:plan": "node scripts/agent-runner.mjs",
    "agents:plan:all": "TASK=$TASK node scripts/agent-runner.mjs dev $TASK && node scripts/agent-runner.mjs qa $TASK && node scripts/agent-runner.mjs security $TASK && node scripts/agent-runner.mjs ops $TASK",
    "agents:apply": "node scripts/apply-plan.mjs",
    "agents:phase": "make full-phase TASK=$TASK",
    "ci:phase": "pnpm type-check && pnpm lint && firebase emulators:exec --only firestore,auth --project demo-nurseconnect \"pnpm -w vitest run\"",
    "ci:local": "pnpm type-check && pnpm -F web build && pnpm -w vitest run && pnpm -F web lint",
    "test": "vitest run -c vitest.config.ts",
 "test:ci": "vitest run -c vitest.config.ts",
    "test:emu": "firebase emulators:exec --only firestore,auth --project demo-nurseconnect \"vitest run -c vitest.config.emu.ts\""
   },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.7.0",
    "@types/node": "^20.19.10",
    "@vitejs/plugin-react": "^5.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "cross-env": "^10.0.0",
    "dotenv": "^17.2.1",
    "firebase-tools": "^14.12.0",
    "node-fetch": "^3.3.2",
    "turbo": "^2.5.5",
    "typescript": "^5.4.5",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "firebase": "^12.1.0",
    "tailwind-merge": "^3.3.1",
    "zod": "^3.25.76"
  }
}