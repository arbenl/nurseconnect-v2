# Firebase Client SDK Configuration (publicly visible)
# Populate these with values from your Firebase project console.
NEXT_PUBLIC_FIREBASE_API_KEY=""
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=""
NEXT_PUBLIC_FIREBASE_PROJECT_ID=""
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=""
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=""
NEXT_PUBLIC_FIREBASE_APP_ID=""

# Firebase Admin SDK Configuration (server-side only, KEEP SECRET)
# For local development, you can use a service account JSON file.
# For production (e.g., Vercel), set these as environment variables in the deployment settings.
# DO NOT commit files with these values populated (.env*.local).
FIREBASE_PROJECT_ID=""
FIREBASE_CLIENT_EMAIL=""
# The private key often has newline characters. When setting as an env var, ensure they are preserved or correctly formatted.
FIREBASE_PRIVATE_KEY=""
